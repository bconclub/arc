anthropic>=0.40.0
supabase>=2.10.0
feedparser>=6.0.11
requests>=2.32.0
python-dotenv>=1.0.0

# video pipeline (install on VPS only — skip on dev if not editing)
# ffmpeg must be installed as system package: apt install ffmpeg
# faster-whisper>=1.0.0
# moviepy>=2.0.0
# Pillow>=10.0.0

# optional: Reddit OAuth (higher rate limits)
# praw>=7.7.0
